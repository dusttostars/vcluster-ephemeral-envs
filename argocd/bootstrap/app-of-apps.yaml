apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ephemeral-platform
  namespace: argocd
  labels:
    ephemeral.io/component: bootstrap
spec:
  project: default
  source:
    repoURL: https://github.com/willsbctm/vcluster-ephemeral-envs.git
    targetRevision: HEAD
    path: argocd/apps
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
