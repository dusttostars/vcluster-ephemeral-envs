FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /ephemeral ./cmd/cli

FROM alpine:3.19
RUN apk add --no-cache git
COPY --from=builder /ephemeral /usr/local/bin/ephemeral
ENTRYPOINT ["ephemeral"]
