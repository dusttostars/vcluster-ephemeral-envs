apiVersion: v1
kind: ResourceQuota
metadata:
  name: team-a-quota
  namespace: tenant-team-a
spec:
  hard:
    requests.cpu: "4"
    requests.memory: "8Gi"
    limits.cpu: "4"
    limits.memory: "8Gi"
    pods: "50"
